import { Card, CardContent, Typography } from "@mui/material";

const Screen = () => {
  return (
    <Card>
      <CardContent>
        <Typography>Test</Typography>
      </CardContent>
    </Card>
  );
};

export default Screen;
